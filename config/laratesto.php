<?php

declare(strict_types=1);

return [
    // Keep Collision's `artisan test` and its coverage thresholds by default.
    // The unified runner is always available as `artisan laratesto:test`.
    'replace_test_command' => false,
];
